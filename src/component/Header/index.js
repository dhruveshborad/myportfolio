import HeaderNavbar from "../HeaderNavbar";

const Header = (props) => {
  return (
    <header className="header">
      <h1>Dhruvesh B.</h1>
      <HeaderNavbar />
    </header>
  );
};
export default Header;
